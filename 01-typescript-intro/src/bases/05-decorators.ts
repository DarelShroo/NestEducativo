/**
 * Ejercicio: Decoradores de Clase
 * 
 * 1. Crea un decorador (función) llamado 'MyClassDecorator'.
 *    Recuerda que un decorador de clase recibe el 'target' (la función constructora) como argumento.
 * 2. Haz que el decorador simplemente haga un console.log del nombre de la clase objetivo.
 * 3. Aplica el decorador @MyClassDecorator a una nueva clase 'SuperPokemon'.
 * 4. Opcional: Investiga y crea un "Factory Decorator" (un decorador que retorna otro decorador) 
 *    para poder pasarle argumentos, por ejemplo: @PrintName('Hola').
 */
