/**
 * Ejercicio 1: Objetos y Arreglos
 * 
 * 1. Crea un arreglo vacío llamado 'pokemons' tipado para contener solo strings.
 * 2. Añade 3 nombres de pokémon usando el método push().
 * 3. Utiliza el método join() para imprimir los nombres separados por comas.
 * 
 * Ejercicio 2: Objetos literales y Desestructuración
 * 
 * 1. Crea un objeto literal llamado 'pikachu' que tenga las propiedades 'id' (number) y 'name' (string).
 * 2. Utiliza la desestructuración de objetos para extraer 'id' y 'name' en constantes independientes.
 * 3. Utiliza el spread operator (...) para crear un objeto 'raichu' copiando las propiedades de 'pikachu', 
 *    pero cambiando su id y nombre.
 */
