/**
 * Ejercicio: Clases y Constructores Simplificados
 * 
 * 1. Crea una clase exportable llamada 'Pokemon'.
 * 2. Utiliza el constructor simplificado para definir:
 *    - Una propiedad pública de solo lectura (readonly) 'id' de tipo number.
 *    - Una propiedad pública 'name' de tipo string.
 * 3. Añade un método (getter) llamado 'imageUrl' que retorne un template string con una URL
 *    ficticia usando el id del pokemon.
 * 4. Añade un método asíncrono 'speak()' que retorne el nombre en mayúsculas.
 * 5. Instancia la clase creando un objeto 'charmander' y muestra en consola su imageUrl y llama a su método speak.
 */
